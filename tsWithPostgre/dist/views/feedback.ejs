<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="/css/feedback.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"
    />
    <%- include('./includes/navbarStyle.ejs') %>
  </head>

  <body>
    <header><%- include('./includes/navbar.ejs') %></header>
    <main>
      <div class="loginBox">
        <h2>Add Your Feedback</h2>

        <form id="feedbackForm">
          <p>What do you think about our application?</p>
          <textarea name="" id="content" cols="50" rows="3"></textarea>
          
           
            <input type="Submit" value="Add Feedback" id="loginBtn" />
        
        </form>
      </div>
      <div class="float-container">
        <%for(feed of feedData) {%>
        <div class="content-box red float-child">
          <p>Username: <%=feed.username%> at <%=feed.createdat%></p>
          
          <p><%=feed.content%></p>
          <input type="hidden" value="<%=feed.feedback_id%>" name="feedId">
          <button
            type="submit"
            class="deleteButton"
            onclick="deleteFeed(this)"
          >
            Delete Feedback
          </button>
        </div>
        <%}%>
       
      </div>
    </main>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/feedback.js" defer></script>
  </body>
</html>
