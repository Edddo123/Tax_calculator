<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="/css/records.css" />
    <%- include('./includes/navbarStyle.ejs') %>
  </head>

  <body>
    <header><%- include('./includes/navbar.ejs') %></header>
    <main>
      <div class="float-container">
        <%for(info of data) {%>
        <div class="content-box red float-child">
          <p>Taxes paid:</p>
          <%for(taxInfo in info.tax) {%>
          <p><%=taxInfo%> = <%=info.tax[taxInfo]%></p>
          <%}%>
          <P>Tax type : <%=info.tax_type%></P>
          <p>Recorded at : <%=info.newDate%></p>
          <input type="hidden" value="<%=info.record_id%>" name="recordId" />
          <button
            type="submit"
            class="deleteButton"
            onclick="deleteRecord(this)"
          >
            Delete Record
          </button>
        </div>

        <%}%>
      </div>
    </main>
    <script src="/js/records.js" defer></script>
  </body>
</html>
